export const themeColors = $state({
  keyword: "#569cd6", // Instructions (e.g. li, add)
  register: "#ce9178", // Registers (e.g. x0, a0)
  directive: "#c586c0", // Directives (e.g. .text)
  number: "#b5cea8", // Integers/Hex
  comment: "#6a9955", // Comments (#)
  string: "#ce9178", // Strings ("...")
  background: "#1e1e1e", // Editor background
});
